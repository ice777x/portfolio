/** @type {import('next').NextConfig} */
module.exports = {
    reactStrictMode: true,
    images: {
        domains: ["images.unsplash.com", "moralev.files.wordpress.com"],
        formats: ["image/webp"],
    },
};
